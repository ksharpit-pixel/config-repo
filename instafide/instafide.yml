spring:
  datasource:
    mysql:
      jdbcurl: jdbc:mysql://localhost/insta
      username: root
      password: root123
    
    postgres:
      jdbcurl: jdbc:postgresql://localhost/instafeed
      username: postgres
      password: root123
# spring.application.mysql.name=instafide

# spring.jpa.properties.hibernate.jdbc.batch_size=500

polar.greeting= local property
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true

# logging:
#   level:
#     org:
#       springframework:
#         web: debug

server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7070/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh

# Define upload locatio
file:
  upload-dir: ./uploads

polar:
  greeting: "Welcome to the catalog from the config server"

